import { FeaturedIndexClient } from "../_pages/FeaturedIndexClient";

export const metadata = {
  title: "Featured · Mi Amor Tales",
  description:
    "A curated room of weddings we return to often. Each opens into its full cinematic page — mehendi, sindoor, the three days.",
};

export default function FeaturedIndexPage() {
  return <FeaturedIndexClient />;
}
